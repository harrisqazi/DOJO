kind = "web"
previewPath = "/"
title = "FormFlow"
version = "1.0.0"
id = "artifacts/formflow"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 8081

[services.development]
run = "pnpm --filter @workspace/formflow run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/formflow", "run", "build" ]
publicDir = "artifacts/formflow/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "8081"
BASE_PATH = "/"
